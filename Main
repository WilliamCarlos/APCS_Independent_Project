/**
 * @author Dean Leitersdorf, William Lee, Ophir Sneh
*/

import javax.swing.JFrame;
public class Main { 
	
	
	public static void main(String[] args) { // Bring up a JFrame with squares to represent the cells
	
	final int DISPLAY_WIDTH = 800; 
	final int DISPLAY_HEIGHT = 560; 
	JFrame f = new JFrame(); 
	f.setSize(DISPLAY_WIDTH, DISPLAY_HEIGHT); 
	Display display = new Display(DISPLAY_WIDTH, DISPLAY_HEIGHT, f); 
	f.setLayout(null); 
	f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); 
	f.setTitle("2D Ball Collisions"); 
	f.add(display); 
	f.setVisible(true);
	f.setResizable(false);
	
	} 
}
